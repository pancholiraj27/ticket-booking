import React from "react";
const MovieImg = ({ img, name }) => {
  return (
    <div className="posterImg posterImgShimmer">
      <img src={""} alt={""} />
    </div>
  );
};

export default MovieImg;
